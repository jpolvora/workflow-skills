---
us: us-65-66
step: 9
reviewDate: 2026-07-17
commit: 7e4f2a4779ca4ce0dac291884729f0498fd836bd
base: 6a4443ecae1f9bad0d8c1178b1f2921bf284c78e
branch: develop
skill: 06-code-review
mode: auto
---

## Code Review

**Branch:** `develop`
**Stack:** `node-skills-hub` (markdown harness docs only)
**Files:** 8
**Commit:** `7e4f2a4` — fix(harness): resolve check-harness phantoms and consumer-safe links (#65 #66)

### AC compliance (AC1–AC7)

| AC | Verdict | Evidence |
|----|---------|----------|
| **AC1** | **PASS** | Packaged `.agents/AGENTS.md` primary Skill index + Task router scoped to Workflows package; Extra skills segregated under `### Extra package (optional)` (L157–183, L211–222). Root `AGENTS.md` L123 dual-scope Drift check footnote added. No Extra ids in primary router tables (grep). |
| **AC2** | **PASS** (minor gap) | `## Skill loading (mandatory)`, `### Precedence`, `### Opt-out` present (L80–105); `using-superpowers` marked `(global — not shipped)`. `caveman` L14, `gabarito` L13+L46, `spec-format` L16 retargeted to `../../AGENTS.md`. Residual: `spec-format` L131 (see Warning). |
| **AC3** | **PASS** | `STEP-DISPATCH.md` L24: `` `Task` `07-integration-validation` ``; L51: `` `09-goal-fix-pr` ``. No bare dispatch ids in touched dispatch table or Step 13 pipeline list. |
| **AC4** | **PASS** | `shared/AGENTS.md` L53: prose + upstream GitHub URL; `rg '../../../bin/skill-dependencies' .agents` → 0 matches. |
| **AC5** | **PASS** | `spec-to-pr/SKILL.md` L18: Hub → `../../AGENTS.md` with dual-hub parenthetical. Path resolves to `.agents/AGENTS.md` from orch skill dir. |
| **AC6** | **PASS** | `spec-to-pr/SKILL.md` L104: link labels `06-code-review` / `08-fix-pr`; targets unchanged. |
| **AC7** | **PASS** (manual) | Scoped manual audit: no phantom Extra routes in primary packaged tables; Skill-loading refs resolve; dispatch prefixed; no broken `bin/` consumer link. Automated `check-harness --dry-run` from `test/` did not complete within timeout (CLI install guard); agent-equivalent checks used instead. |

### Critical

_(none)_

### Warning

- **`.agents/skills/spec-format/SKILL.md:L131`**: WARNING — References § still links root hub `../../../AGENTS.md` for harness routing; plan Step 2 explicitly listed L131 for packaged-hub retarget alongside L16 (Score: 6/10)
  - **Analysis:** Evidence read: diff (L16 changed, L131 untouched), plan manifest §Exact file manifest, `spec-format/SKILL.md` References block. Failure scenario: Workflows-only consumer with product-specific root `AGENTS.md` — agent following References loads wrong hub for routing. Missing protection: no link-resolution test for all utility-skill `AGENTS.md` cites. Discards: not intentional dual-hub (L16 proves packaged intent); not pre-existing acceptable pattern for References harness line.
  - **Sibling occurrences:** none in diff scope; `gabarito/README.md` L21 similar but out of plan manifest.
  - **Fixable:** retarget L131 to `[../../AGENTS.md](../../AGENTS.md)` (surgical one-line).

### Suggestion

- **`.agents/skills/gabarito/SKILL.md:L23`**: SUGGESTION — Style § still cites `../../../AGENTS.md` § Language while packaged hub holds `### Language (mandatory)` under Rules (L34); partial AC2 retarget left non–Skill-loading cites on consumer root (Score: 5/10)
  - **Analysis:** Pre-existing pattern partially updated (L13, L46 fixed). Consumer product root may lack § Language. Low severity — autoload contract already on packaged hub.
  - **Fixable:** optional — point Language cite to `../../AGENTS.md` Rules § Language or packaged hub anchor.

- **`.agents/skills/spec-to-pr/README.md`**, **`11-ship-pr/SKILL.md`**: SUGGESTION — Unprefixed prose/link labels (`goal-fix-pr`, `code-review`, `fix-pr`) remain outside diff manifest; out of surgical scope per plan but may surface in future check-harness Phase 5 scans (Score: 4/10)

---

## Portability & regressions

| Check | Result |
|-------|--------|
| Hardcoded consumer org/repo | None introduced |
| Absolute / author-machine paths | None |
| Broken `bin/` relative links in `.agents/` | None (`rg` clean) |
| Phantom Extra skills in primary packaged router | None |
| Unprefixed STEP-DISPATCH dispatch ids (touched file) | None |
| Scope creep (installer, check-harness body, site) | None — surgical markdown diff only |

## Karpathy-guidelines alignment

- **Surgical:** 8 files match plan manifest; no drive-by refactors.
- **Simplicity:** Restructure mirrors root hub patterns; Extra subsection is minimal segregation.
- **Verifiable:** AC1–AC7 mapped to concrete line evidence.

---

**Verdict:** **APPROVE** — no Critical findings; one fixable Warning (optional before ship). Proceed to Step 10 only if orchestrator gates on Warning (autoMode may advance).

**Apply fixes?** NO (report-only per auto Step 9).
