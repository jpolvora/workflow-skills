---
slug: spec-provider-skills
step: 11
title: "Integration test plan — spec-provider-skills"
mode: auto-full
skipBrowser: true
sourcePlan: step-02-spec-provider-skills.plan.refined.md
sourceSpec: step-00-spec-provider-skills.spec.md
createdAt: "2026-07-13T14:50:00Z"
---

# Integration Test Plan — spec-provider-skills

Stack: **node-skills-hub** (Node 22 CLI + Markdown skills). No app API, database, RBAC, or UI runtime. Battery maps AC8/AC9 + packaging canonicity to `config.json.verification` and shim smoke. Browser MCP: **skipped** (`[AUTO][FULL]` / `skip-browser`).

## 1. Target Hosts & Ports

| Target | Value | Notes |
|--------|-------|-------|
| API host/port | N/A | `stack.backend.apiHost` empty; no HTTP service |
| Dev host/port | N/A | `stack.frontend.devHost` empty; static `docs/` only |
| Site catalog | local `docs/index.html` | Regenerated by `node bin/build-site.js` |
| Package under test | local tarball via `npm run tests -- --local` | Consumer install in temp dir |

Credentials: none required for this battery. Live GH/ADO `fetch-to-spec` / PAT auth is **out of battery** (optional pre-PR manual smoke; not AC gate for hub packaging).

## 2. Database State & Seeding

| Item | Status |
|------|--------|
| Database type | `none` |
| Migrations | N/A |
| Seeds / rollback | N/A |

No DB steps. Artifact cleanup for tests is owned by `test/test-install.js` (temp consumer dirs).

## 3. API Contracts

No REST/GraphQL surface under test. Contract checks are **script CLI + install canonicity**:

| Check ID | Command / assert | Expected |
|----------|------------------|----------|
| IT-BUILD | `node bin/build-site.js` | exit 0; `docs/index.html` lists three providers |
| IT-PACK | `npm run tests -- --local` | exit 0; canonicity + install + update `--include-new` |
| IT-SHIM-GH | `python .agents/skills/spec-to-pr/scripts/github-issue-to-spec.py --help` | exit 0; help text (forward to canonical) |
| IT-SHIM-ADO | `python .agents/skills/spec-to-pr/scripts/ado-workitem-to-spec.py --help` | exit 0; help text |
| IT-SHIM-FETCH | `node .agents/skills/08-fix-pr/scripts/fetch_threads.cjs --help` (or usage) | exit 0 or documented usage; no crash |
| IT-SHIM-RESOLVE | `node .agents/skills/08-fix-pr/scripts/resolve_thread.cjs --help` (or usage) | exit 0 or documented usage; no crash |
| IT-SHIM-ADO-CTX | `python .agents/skills/08-fix-pr/scripts/fix_pr_azure_context.py --help` | exit 0; help text en-us |
| IT-DUAL | Spot-check headings in three provider `SKILL.md` | `### Standalone Mode` + `### Workflow Mode` present |

## 4. RBAC & Tenancy Isolation

N/A for skills hub. Security spot-checks (static, non-blocking if already covered by Step 6/9):

| Check | Expect |
|-------|--------|
| PAT only via env | Provider SKILLs document `ADO_PAT` / `patEnvVar`; no hardcoded tokens in scripts |
| `config.json` gitignored | Not staged; schema + example committed only |

## 5. UI & Browser Paths

**Skipped** (`skip-browser` / no browser MCP). Static catalog only:

| Check | Expect |
|-------|--------|
| Catalog regeneration | `build-site` succeeds |
| Provider names in catalog | `github-provider`, `azure-devops-provider`, `local-spec-provider` appear in `docs/index.html` |

No form fields, locales beyond en-us docs, or a11y browser checks in this run.

## 6. Defect Thresholds

| Metric | Pass | Fail / escalate |
|--------|------|-----------------|
| IT-BUILD | exit 0 | FAIL → report; critical fix max 3 if site broken |
| IT-PACK | exit 0, all phases green | FAIL → report; do not advance Step 12 until green |
| Shim `--help` suite | all shims runnable | FAIL if shim missing or forward broken (AC9) |
| Dual-mode headings | 3/3 providers | FAIL if any provider missing Standalone/Workflow |
| Browser cases | N/A (skipped) | Do not count as fail |
| Live remote fetch | optional, not executed | Do not count as fail |

**Overall verdict:** PASS only if IT-BUILD, IT-PACK, shim smoke, and dual-mode spot-check all pass.

## Execution order

1. Write this plan (done).
2. Run IT-BUILD.
3. Run IT-PACK (`npm run tests -- --local`).
4. Run shim `--help` smoke (IT-SHIM-*).
5. Spot-check dual-mode headings (IT-DUAL).
6. Grep catalog for providers (post-build).
7. Write `step-11-spec-provider-skills.integration-test.report.md`.

## Mapping to acceptance criteria

| AC | Covered by |
|----|------------|
| AC1 dual-mode | IT-DUAL (+ pack canonicity) |
| AC8 tests + site | IT-PACK, IT-BUILD |
| AC9 shims | IT-SHIM-*, pack asserts |
| AC2–AC7, AC5–AC6 | Prior Step 6 report; this battery does not re-audit docs unless pack fails |
