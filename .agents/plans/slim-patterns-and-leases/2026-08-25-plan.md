# Slim patterns + session leases Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Drop backend/frontend pattern memory and agent lock/lease from shipped skills so workflows stay lighter; keep `ws-self-learning` MEMORY.

**Architecture:** Interpret `todo.txt` against the live tree, not the literal skill ids. `ws-backend` / `ws-frontend` already merged into `ws-patterns`. Session leases live in orch bootstrap + `session_lease.cjs`, not a separate skill. `ws-audit` is already gone from the product tree.

**Tech Stack:** Node 22 skill package, `bin/skill-dependencies.json`, hub `config.json` schema, orch SKILL.md / setup.md / tools.md.

## Global Constraints

- Language: en-us for skill bodies, gates, banners, harness docs.
- Edit SoT only under `$PWD/.agents/skills/ws-*`. Never write `{globalSkillsRoot}`.
- Do not rewrite archived `{plansDir}` / `{specsDir}` history.
- Keep `ws-self-learning` / `MEMORY.md` (user will replace pattern memory later).
- `ws-shared/AGENTS.md` stays under 14000 UTF-8 bytes.
- Single package version bump after the functional diff (`0.3.37` → `0.3.38`).

---

## Interpretation (from `todo.txt`)

| User line | Live artifact | Action |
|-----------|---------------|--------|
| remover ws-backend/frontend | `ws-patterns` + `{sharedDir}/backend.md` / `frontend.md` consult + `defaults.patternsBackend` / `patternsFrontend` | Delete skill; stop consult/record; stop seeding templates; leave leftover consumer files inert |
| remover marcacao de lock / lease | `defaults.sessionLeases`, `session_lease.cjs`, `git.lock`, slug leases, Invariant 9 | Delete script + schema; unwrap git recipes; remove config key |
| remover referencias | Live skills, hubs, installer, tests, FEATURES, README, site | Strip product references; do not rewrite old plan archives |
| enxugar orch | Proof fields `pattern_consult`, lease heartbeat/acquire/release, enhancing-skill list | Drop those obligations; keep `memory_consult` |
| confirme se ja removido ws-audit | No `.agents/skills/ws-audit/`; no `test-ws-audit.js`; FEATURES 0.3.37 + CHANGELOG already record removal | Confirm only; no extra product work |

**Out of scope:** historical specs/plans mentioning retired ids; Extra skills; fable/judge; MEMORY engine.

---

## File map

**Delete**

- `.agents/skills/ws-patterns/` (SKILL.md + evals)
- `.agents/skills/ws-spec-to-pr/scripts/session_lease.cjs`
- `.agents/skills/ws-shared/session-lease.schema.json`
- `.agents/skills/ws-shared/backend.md.template`
- `.agents/skills/ws-shared/frontend.md.template`
- `test/test-pattern-consult.js`
- `test/test-session-lease.js`

**Keep on disk, stop consulting:** this repo's dogfood `backend.md` / `frontend.md` (consumer-owned; installer still must not overwrite existing copies).

---

## Task 1: Remove `ws-patterns`

- [ ] Delete skill folder and evals
- [ ] Drop from `bin/skill-dependencies.json` and hub copy (`workflows` list + orch deps)
- [ ] CATALOG.md / ws-shared/CATALOG.md / README / FEATURES inventory + task router
- [ ] `autoload.md` Always-applied + precedence; `configure_autoload.py` `DEFAULT_ALWAYS_APPLIED`
- [ ] Pipeline: `ws-implement-tasks`, `ws-write-plan`, `ws-code-review`, `ws-self-learning`, `ws-goal-fix-pr`, `ws-fix-pr` COOPERATIVE_FIX/AUTO_FIX
- [ ] Orch: PROTOCOLS.md, STEP-DISPATCH.md, lite SKILL.md invariant 10 (keep MEMORY consult only)
- [ ] `build_dispatch_context.cjs` / `measure_harness.cjs` enhancing list
- [ ] Config: schema, example, local `config.json` keys `patternsBackend` / `patternsFrontend`
- [ ] `ws-configure-project`: drop `--section patterns` and interview flags
- [ ] Installer: stop seeding backend/frontend from templates; drop templates from `HUB_WHITELIST`; keep `CONSUMER_OWNED` names so update does not clobber leftovers
- [ ] Tests: delete `test-pattern-consult.js`; invert `test-install.js` membership; remove `ws-patterns` from autoload configure fixtures
- [ ] `package.json` test scripts drop `test-pattern-consult.js`

## Task 2: Remove session leases / git lock

- [ ] Delete `session_lease.cjs` and `session-lease.schema.json`
- [ ] Config: drop `defaults.sessionLeases` from schema, example, local config
- [ ] `config-resolution.md` delete Session lease section
- [ ] `setup.md` delete §5a; drop `leaseId` from 5b state write; unwrap 5b git-lock wording
- [ ] `tools.md` unwrap `commit-code` / `push-branch` / `revert-to-checkpoint` git-lock
- [ ] Orch invariants 5/9 (standard) and 9 (lite); `ws-ship-pr` git-lock paragraph
- [ ] `HUB_WHITELIST` drop `session-lease.schema.json`
- [ ] Delete `test-session-lease.js`; drop from `package.json` scripts

## Task 3: Confirm `ws-audit`

- [ ] Assert no `.agents/skills/ws-audit/` and no `test-ws-audit.js`
- [ ] Assert no live skill body / schema `enableAuditing`
- [ ] Report: already removed in 0.3.37 (FEATURES + CHANGELOG). Historical specs stay.

## Task 4: Docs, bump, verify

- [ ] FEATURES.md current inventory + new 0.3.38 row; skill count 48 → 47
- [ ] CATALOG scope note `workflows` 42 → 41
- [ ] Root AGENTS.md session contract: drop pattern-file writes on fix-pr
- [ ] `ws-shared/AGENTS.md` consumer-owned table
- [ ] Changelog append (do not rewrite past entries)
- [ ] `npm run build-site:bump` (0.3.38) then `npm run generate-integrity`
- [ ] `npm run test` + `node test/test-context-budget.js`

---

## Verify

1. `node test/test-install.js --local` — workflows package does not install `ws-patterns`
2. `node test/test-autoload-configure.js` — Always-applied has no `ws-patterns`
3. `rg ws-patterns .agents/skills README.md FEATURES.md CATALOG.md bin test --glob '!**/plans/**' --glob '!**/specs/**'` — no live product hits
4. `rg session_lease|sessionLeases|git-lock .agents/skills bin test` — no live product hits
5. Full `npm run test`
