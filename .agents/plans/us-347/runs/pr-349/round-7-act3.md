# PR 349 goal-fix-pr — round 7 (Act round 3 dispatched)

- Observed: activeThreads = 3, all score 7 fix-code, same defect class (learning-write ownership unconditional vs lite/Tier-3 with no batch worker): PRRT_kwDOTFajc86j5cKi (L81 Step 5), PRRT_kwDOTFajc86j5cLO (L116 Subagent contract), PRRT_kwDOTFajc86j5cLy (L99 Ownership split). Checks = test pass x2, review **fail** (3 unresolved threads).
- Triage: review fail = **diff-regression** (gate on the 3 threads; anchors are lines this PR introduced — worker to confirm each via git diff main...HEAD). Same unconditional-contract class as batches 1-2 (third quoter wave). Not baseline/flake → Act round 3.
- Revision: 3. Blocked reason: empty (new reason → counter reset).
- Dispatch: FRESH worker, batch = all 3 threads (one ordered pair for the whole batch), gate = .agents/plans/us-347/runs/pr-349/plan-gate-r3.md, round = 3. Roles muse-spark/muse-spark.
- Worker scope: ws-fix-pr once for PR 349; verify each premise + each anchor line is PR-changed; CRITICAL: test/test-goal-fix-pr-orchestrator-dispatch.js L236-239 allegedly asserts the unconditional learning wording — read the lock before rewording; test file is PR-added so lock updates are in surgical scope if the wording must change, but prefer pure-insertion scoping that keeps existing assertions green; proactive sweep must be EXHAUSTIVE this time — every remaining unconditional "batch worker / dispatch / telemetry / Learning" ownership statement in the whole SKILL.md (batches 1-2 swept only the dispatch section and missed these); verify/commit/push/resolve all 3.
