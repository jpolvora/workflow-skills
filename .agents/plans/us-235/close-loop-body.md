PR: https://github.com/jpolvora/workflow-skills/pull/237

Shipped us-235 on `feature/us-235`: comment verification keys are never required aliases; `skipReason` counts as observed without knownDefect; missing aliases still fail pre-advance 6; `.runtime` allows cjs/patch/md; state hash is frontmatter-only; `finish --commit` records G2-code SHAs. Derived score 9/10. Parent owns review-thread fix and merge.
