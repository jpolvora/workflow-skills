Merged https://github.com/jpolvora/workflow-skills/pull/237 into main.

us-235 is delivered: comment verification keys are never required aliases; skipReason counts as observed without knownDefect; missing aliases still fail pre-advance 6; .runtime allows cjs/patch/md; state hash is frontmatter-only with legacy full-file digest accepted until the next update; finish --commit records G2-code SHAs. Review threads reached zero and CI (review + test) completed green.
