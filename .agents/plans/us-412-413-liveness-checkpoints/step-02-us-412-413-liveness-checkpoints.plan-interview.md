---
slug: us-412-413-liveness-checkpoints
title: "Plan interview — Unattended autoMode runs: mid-step checkpoints, turn-boundary pause state, and monitor stall detection"
status: completed
step: 2
workflowId: us-412-413-liveness-checkpoints-20260924T043148Z
startedAt: "2026-09-24T05:02:00Z"
endedAt: "2026-09-24T05:06:06Z"
autoMode: true
rounds: 1
blocking_open: 0
shared_understanding: confirmed
acRefs: []
---
# Plan interview (Step 2) — `us-412-413-liveness-checkpoints`

Adversarial audit of [`step-01-us-412-413-liveness-checkpoints.plan.md`](step-01-us-412-413-liveness-checkpoints.plan.md) against [`step-00-us-412-413-liveness-checkpoints.spec.md`](step-00-us-412-413-liveness-checkpoints.spec.md) (19 ACs, NS1–NS6), `.runtime/plan.index.json`, the live codebase, `.ws/STACK.md`, `.ws/MEMORY.md`, the spec-memo vault, and the Step-1 observer evidence ([`observer/window-1-report.md`](observer/window-1-report.md)).

- **Mode:** `autoMode: true`, `fullMode: true`, `dryRun: false` — no interactive user. Every open question (plan §8 Q1–Q9) is resolved here with the plan's recommended option and recorded as `autoMode: auto-confirmed`; every additional gap found by the audit (G1–G16) is resolved with project evidence or best technical judgment (`resolutionSource` recorded). No silent gaps.
- **Outcome:** `blocking_open: 0`; `shared_understanding: confirmed`; refined plan written to [`step-02-us-412-413-liveness-checkpoints.plan.refined.md`](step-02-us-412-413-liveness-checkpoints.plan.refined.md). `step-01-*.plan.md` is untouched.
- **Anchor:** `uswf/us-412-413-liveness-checkpoints-20260924T043148Z/before-step-2` @ HEAD `4fcc0f99675c16ca8593a86fba3687967449955b` (tag present, verified).
- **Memory gate:** `check_memory_conflict.cjs <plan> --json` ran; no `force_interview` conflict surfaced for the plan paths (the Step-1 finish already recorded `memory-conflict exit 0`).

## Verdict summary

| Metric | Value |
|--------|-------|
| Questions from plan §8 | 9 (Q1–Q9) — all resolved, `autoMode: auto-confirmed` (Q6 confirmed with one evidence-backed clarification) |
| Additional gaps registered | 17 (G1–G17) — 3 blocking (G1, G2, G6), 14 non-blocking; all closed |
| Blocking gaps open | 0 |
| ACs audited | AC1–AC19 all mapped in the refined plan (§1 + §5); no AC left without a file, a task, and a test/NS |
| Negative scenarios | NS1–NS6 each mapped to a named red-before/green-after test (refined §5) |
| Section 6 invariants plan | Present and strengthened (state-writer, schema/input validation, observer read-only, portability, consumer-data isolation) |
| Existing-suite compatibility | `test/test-ws-monitor-us356.js` is the binding compatibility anchor (stall @ line ~286; `scan-capped` @ ~401; capped/`load-probe` @ ~478–500) — G6 prevents its regression |
| Refinement rounds | 1 (this audit) |

## Interview registry — plan §8 open questions

| # | Question | Recommended default (plan) | Resolution | resolutionSource | autoMode |
|---|----------|----------------------------|------------|------------------|----------|
| Q1 | `--progress` grammar: structured JSON vs compact shorthand | JSON `{substep, completedUnits, remainingUnits}`, fail-closed parse; shorthand rejected | JSON grammar kept and fail-closed. Added `--progress-file <path>` as an equivalent, mutually exclusive input (see G2) so the documented recipe stays shell-portable; tests use argv arrays (no shell). | model-inferred + project (`CROSS-PLATFORM.md` rule 2; MEMORY PowerShell quoting traps) | auto-confirmed |
| Q2 | `pause-turn` `nextAction` source | Explicit `--next-action` wins; derive from `state.stepCheckpoints[N]` when absent; error when neither exists (AC9) | Kept exactly; docs must show `--next-action "Finish step N"` for the no-checkpoint case. Field read renamed with G1. | project (AC9 fail-closed) + model-inferred | auto-confirmed |
| Q3 | Pause cleared by `finish` of a different step | Clear only when `turnPause.step` equals the finished step; leave other-step pauses intact | Kept. Same rule applied to `stepCheckpoints[step]` deletion (only the finished step's record). | project (AC5 wording) | auto-confirmed |
| Q4 | `--until-terminal` without `--slug`/`--workflow-id` | Exit when the scoped report has no active workflow (`activeCount === 0`); with a selector, that is the selected workflow | Kept; "non-active" means outside `active\|blocked\|in_progress` (same set the stall gate uses) so a `blocked` run is not treated as terminal (G5). | project (monitor `isActive` set, line 1509) | auto-confirmed |
| Q5 | Pause finding code/severity | `worker-session-paused` (info) replacing the suppressed `worker-session-stall`; expose `turnPause` on the workflow record | Kept. Suppression is presence-based while the workflow is active; docs state the marker is written only on a real turn-boundary pause (G9). | project (AC15) + model-inferred | auto-confirmed |
| Q6 | Per-root budget shape / `capped` semantics | Dynamic reservation `max(1, floor(remaining/remainingRoots))` + correlation-first ordering; only global per-tick stops/truncation set `capped`; correlated root always visited | Kept, with one clarification: **per-root slice truncation also sets `capped`** — otherwise `test/test-ws-monitor-us356.js` (~line 490, `load-probe` 210-file root expects `capped === true`) regresses, and the flag would under-report a scan that stopped before exhausting candidates. Budget math unchanged. | project (`test-ws-monitor-us356.js` AC6 block; AC13 honesty) | auto-confirmed (clarified) |
| Q7 | State shape when no checkpoint/pause exists | Omit `stepCheckpoints`/`turnPause` entirely; schema properties optional | Kept (field renamed per G1). `finish` deletes an emptied map rather than writing `{}`. | model-inferred (additive-only schema) | auto-confirmed |
| Q8 | `gates.md` one-line `autoMode`/turn-boundary nuance | Include it (consistency line; no behavior change) — out of the strict AC8 file list | Kept: one scoping line at `gates.md:52` ("proceeds continuously across step boundaries" applies within a host turn that can run them; otherwise checkpoint + pause). | model-inferred (docs consistency; no behavior change) | auto-confirmed |
| Q9 | `update_state.cjs` wrapper edit | No edit expected (forwards argv); if an explicit op list is demanded, keep the diff wrapper-local and minimal | Kept: wrapper stays a thin forwarder (verified `ws-spec-to-pr/scripts/update_state.cjs`); operation allowlist and usage help are updated in `workflow_state.cjs` `runUpdateCli` (lines 2043–2051). | project (wrapper code read) | auto-confirmed |

## Gap registry — audit findings

Class: **blocking** = prevents development or changes an AC/regresses a contract; **non-blocking** = quality/consistency, resolved by default.

| ID | Class | Section | Gap | Resolution | resolutionSource | Evidence | dependsOn |
|----|-------|---------|-----|------------|------------------|----------|-----------|
| G1 | blocking | §2.1 / §2.2 | **Field-name collision.** Plan stores the mid-step map in `state.checkpoints`, but `checkpoints` is already a documented state field holding the **git-tag mirror array** (`PROTOCOLS.md:257` "Mirror in `checkpoints[]`"; state-shape example `PROTOCOLS.md:281`; legacy state files carry `checkpoints: []` / arrays, e.g. `.agents/plans/installer-antigravity-gemini-skills-json/installer-antigravity-gemini-skills-json.state.json:350`). A new fail-closed "checkpoints must be a record map" check in `validateSnapshot` would fail on any resumed legacy state, and two different `checkpoints` shapes would share one name. | **Rename the new map to `state.stepCheckpoints`** (object keyed by step string). `state.turnPause` unchanged. Legacy `checkpoints[]` is left untouched (no migration shim). AC1/AC5 name no field, so the rename is AC-compatible; the spec § Assumptions default (`state.checkpoints`) is superseded and recorded here. | project (legacy states + PROTOCOLS docs; `rg` shows no current code writer) | `.agents/skills/ws-spec-to-pr/PROTOCOLS.md:257,281`; `rg "checkpoints" .agents/skills --glob "*.cjs"` → no writer | — |
| G2 | blocking | §2.1 | **Inline JSON on the command line violates the repo's own cross-platform rule.** `CROSS-PLATFORM.md` PowerShell rule 2 forbids inline JSON ("the shell strips quotes: use key=value flags, payload files, or an in-process driver"); MEMORY records repeated PowerShell quoting failures (`2026-09-21-powershell-quoting-one-liners`, `2026-09-22-us-386-powershell-cli-vocabulary`). The documented `--progress '<json>'` recipe is therefore not portable on the primary (win32) host. | Keep `--progress` inline JSON **and add `--progress-file <path>`** (same fail-closed parse; mutually exclusive with `--progress`). Docs/SKILL recipes use `--progress-file`; tests invoke the wrapper with argv arrays (`spawnSync`, no shell). `check_shell_quoting.cjs` only scans `node -e` / `python -c` recipes, so both forms pass Phase 5a. | project (`CROSS-PLATFORM.md`; MEMORY traps) + model-inferred | `.agents/skills/ws-shared/runtime/CROSS-PLATFORM.md` § PowerShell rules; `ws-check-harness/scripts/check_shell_quoting.cjs` scope | Q1 |
| G3 | non-blocking | §2.1 / §2.4 | `turnPause.nextAction` (resume unit) shares a name with the top-level `state.nextAction` ("Finish step N", written by `performUpdate`). Silent ambiguity in docs/monitor output. | Keep the nested name; docs and monitor output always qualify it (`state.turnPause.nextAction` vs `state.nextAction`); refined plan states the distinction once. | project (`workflow_state.cjs:1559`, state shape) | `workflow_state.cjs:1559` (`state.nextAction = 'Finish step N'`) | — |
| G4 | non-blocking | §2.1 / §3.2 | `finish` idempotency: `priorFingerprint`/`finishFingerprint` is computed **before** mutation (`workflow_state.cjs:1454`), so clearing `stepCheckpoints`/`turnPause` must not perturb a repeat `finish` (fingerprint stability is the idempotency contract). | Clear markers after fingerprint capture and before `canonicalStateJson`; add a regression that a second identical `finish` leaves revision/fingerprint behavior unchanged (T9). | project (code read) | `workflow_state.cjs:1453-1456` | — |
| G5 | non-blocking | §2.3 | `--until-terminal` "non-active" definition could treat `blocked` as terminal (it is not: `blocked` is still a live, resumable run). | Define terminal as outside `active\|blocked\|in_progress` (mirrors the stall gate's `isActive`, line 1509) for both the selected workflow and the `activeCount === 0` no-selector case. | project (code) | `monitor_snapshot.cjs:1509,1538` | Q4 |
| G6 | blocking | §2.3 / §3.4 | **`capped` semantics under per-root reservation.** If only global collection/read caps set `capped`, the existing `test/test-ws-monitor-us356.js` capped block (210-file `load-probe` root, asserts `capped === true` and `scan-capped` for a zero-candidate workflow while a scanned session stays `available`) regresses: reservation distributes the budget so the global 200-file cap is never reached. It would also under-report truncation. | `capped = true` when (a) the global file cap is reached during collection, (b) the read loop hits the time/byte cap, **or (c) any root's candidate slice was truncated** (candidates left uncollected). `resolveTranscriptSource` keeps its rule: zero candidates + `capped` → `scan-capped`; a read match always resolves `available` (AC12/AC13). | project (`test-ws-monitor-us356.js` AC6 block ~lines 478–500; AC13 honesty) | `test/test-ws-monitor-us356.js:478-500`; `monitor_snapshot.cjs:1159,1163,1315` | Q6 |
| G7 | non-blocking | §2.3 / §3.4 | Root ordering: the Step-1 dogfood window reproduced `filesScanned: 0` **even with an explicit `--transcript-root`** plus discovery (`observer/window-1-report.md` F1), and `resolveCandidateTranscriptRoots` currently emits config roots → explicit roots → workspace → host roots (`monitor_snapshot.cjs:1031-1100`). | Rank roots before the walk: (1) roots whose path correlates with the workflow key, (2) explicit `--transcript-root` roots, (3) config `monitor.transcriptRoots`, (4) workspace candidates, (5) host roots; per-root reservation then applies in that order. Keep the read-only/opt-in posture. | project (dogfood F1 + code read) | `monitor_snapshot.cjs:1031-1100`; `observer/window-1-report.md` §F1 | — |
| G8 | non-blocking | §2.3 / §3.5 | **Shared window must be the exact matched bytes.** The plan stores the **raw** read window (`tail: rawText`) for resolve while the scan filter also matched raw text; raw host text would then be retained in memory by an exported function (`scanTranscriptRoots` is exported, line 1722) and only dropped before serialization. Conversely, storing only the sanitized text while filtering on raw could produce scan/resolve disagreement (AC12). | Make it strictly one window: sanitize first, then run the correlation filter on the **sanitized** text, then store that same sanitized window as `tail`; `resolveTranscriptSource` re-matches the same bytes. Keep `delete scanned.tail` before serialization as defense-in-depth, and keep `sanitizeTranscriptText`'s secret/home-path guarantees intact. Correlation keys are slug/workflowId (identifier-shaped), so sanitization cannot drop a legitimate match. | project (AC12 wording; privacy posture in `observer-instructions.md`; exported helper) | `monitor_snapshot.cjs:1167-1189,1300-1326,1722`; `observer-instructions.md` § Read-only posture | — |
| G9 | non-blocking | §2.3 / §2.4 | **Speculative pause would permanently suppress stall detection** (task risk note; BR7 has no TTL). | Contract rule in `SKILL.md`/`PROTOCOLS.md`: `pause-turn` is written **only** when the turn actually ends mid-step (never as a pre-emptive heartbeat); suppression is presence-based while the workflow is active; the terminating `finish` (or the resume flow's `finish` for that step) clears it; `dispatch` never writes or clears markers. Regression: dispatch leaves `turnPause`/`stepCheckpoints` untouched; a paused workflow that resumes and finishes is un-suppressed afterwards (T9/M5). | model-inferred + project (AC15, BR7) | spec § Design Intent "Rejected alternative"; BR7 | Q5 |
| G10 | non-blocking | §2.2 / §3.3 | `validate_json_schema.cjs` `validateNode` supports `properties`/`required`/`additionalProperties:false`/`items`/`enum`/`oneOf` but **not `patternProperties` or `$ref`** (verified). A step-keyed map cannot be validated by the schema file alone. | Keep schema declarations shallow (`stepCheckpoints: {type:'object'}`, `turnPause` with `additionalProperties:false` + required fields); enforce record shapes (step key 0..9, `substep` string, `completedUnits` string[], `remainingUnits` int ≥ 0, ISO `updatedAt`; `turnPause.step` in range, ISO `at`, non-empty `reason`/`nextAction`) in `validateSnapshot` code (NS6). Tests must assert the code path, not schema-only rejection. | project (validator code read) | `.agents/skills/ws-shared/runtime/scripts/validate_json_schema.cjs:28-95` | — |
| G11 | non-blocking | §2.1 / §2.2 | Markdown dual-write must render nested record maps (`serializeFrontmatter` → `inlineObject`/`formatInline` handles nested arrays/objects, but the new shapes are untested). | Assert in T5 that after `checkpoint`/`pause-turn` the `.state.md` frontmatter still parses (`parseFrontmatter`), `stateCoresAgree` holds, and `validate_state.cjs` exits 0; render records readably (inline objects are acceptable). | project (code read) | `workflow_state.cjs:534-556` (`serializeFrontmatter`, `inlineObject`), `:154-157` (`stateCoresAgree`) | — |
| G12 | non-blocking | §2.5 / §3.8 | Monitor fixture isolation: MEMORY traps — an unfiltered monitor assertion in a temp root with positive fixtures leaks (`2026-09-21-edit-tool-multiline-matches-fail-on-crlf-files` context / monitor fixture leak), and `--transcript-root` does not replace discovery. | Every new monitor test passes `--workflow-id` (or `--slug`) scoping the fixture and isolates host stores via `monitor.hostHome`; positive/negative fixtures carry distinct workflow ids; never assert absence in a shared temp root. | project (MEMORY) | `.ws/memory/2026-09-21-monitor-subagent-recovery-correlation.md`; `.ws/memory/2026-09-21-edit-tool-multiline-matches-fail-on-crlf-files.md` (fixture-leak entry) | — |
| G13 | non-blocking | §2.3 / §3.6 | `--until-terminal` loop/parse mechanics: current `main()` requires `--iterations` with `--watch` (line 1666) and loops on `iterations` (line 1691); NS4 requires a usage error with **no scan** for bad combinations. | Validate in `main()` before the first `snapshot()`: `--until-terminal` requires `--watch`; mutually exclusive with `--iterations`; `untilTerminal` added to the boolean-token list in `parseArgs`; loop exits after a snapshot whose scoped report has no active workflow; plain `--watch` keeps its `--iterations` requirement; help text updated. | project (code read) | `monitor_snapshot.cjs:290-320,1660-1691` | Q4, G5 |
| G14 | non-blocking | §2.4 / §3.7 / §3.9 | Docs/site/hub sync + naming disambiguation: `PROTOCOLS.md` § Checkpoints currently documents the git-tag mirror (`checkpoints[]`) — the new mid-step contract must not be conflated with it; `observer-instructions.md` taxonomy gains the pause finding and the single-window statement; README/FEATURES/site follow the mandatory update rule. Root `AGENTS.md` § Upstream session contract snapshot (0.4.33) does not enumerate `update_state` operations → no root-hub edit expected; `.ws/AGENTS.md` consumer hub is untouched (consumer-owned). | Refined plan §2.4/§3.7/§3.9 lists exact files and statements; `PROTOCOLS.md` gains a "Turn-boundary pause & mid-step checkpoints" subsection distinct from the git-tag `Checkpoints` section, and the state-shape example gains `stepCheckpoints`/`turnPause`. | project (docs read; harness change protocol) | `PROTOCOLS.md:255-261,281`; `observer-instructions.md` § Transcript vocabulary / Finding classification; root `AGENTS.md` § Harness change protocol | G1 |
| G15 | non-blocking | §3.9 / §7 | Upstream release obligations and their ordering (MEMORY): one version bump per release PR strictly above merge-base (`2026-09-19-ship-version-bump-when-equal-base`); integrity regenerated **after the last product edit** on a clean tree (`2026-09-09-integrity-after-final-skill-edit`, `2026-09-22-us-395-terminal-close-and-integrity`); ledger/file-evidence linked after final edits (`2026-09-22-us-402-ship-verify-order`, `2026-09-21-g2-completeness-ledger-boundary`); GUI editor sync N/A (no config keys). | Refined plan §3.9/§7 keep these as explicit ordered steps with commands; post-review-fix edits re-trigger integrity regen + ledger re-link before re-running the alias. | project (MEMORY) | `.ws/memory/2026-09-09-integrity-after-final-skill-edit.md`; `.ws/memory/2026-09-19-ship-version-bump-when-equal-base.md`; `.ws/memory/2026-09-22-us-395-terminal-close-and-integrity.md`; `.ws/memory/2026-09-22-us-402-ship-verify-order.md` | — |
| G16 | non-blocking | §3 defect-class sweeps | Sibling-sweep completeness: the plan's budget-class sweep should also cover `expandMuseSessionDirs` (`walkCap = 500`, `limit = 50`, `monitor_snapshot.cjs:97-122`) and the ordering of `resolveCandidateTranscriptRoots`, which are the other unbounded-collection paths feeding the same starvation shape. | Refined plan extends the sweep with explicit checks and expected findings (reservation applies at the scan root loop; caps inside adapters only bound root enumeration, not per-tick file reads). | project (code read) | `monitor_snapshot.cjs:97-122,1031-1100` | G7 |
| G17 | non-blocking | `.runtime/plan.index.json` hygiene | `plan.index.json` `source` records `sha256 46c11fc8…, 31662 B` for `step-01-*.plan.md`, but the current file hashes `a840f0e8…, 31800 B` (frontmatter was stamped `status: completed` / `endedAt` after the index snapshot; `stampStepArtifact` runs inside `finish`, `workflow_state.cjs:1816`). No pre-advance check consumes the source sha (`validateSnapshot` requires only that the index exists and is a parseable JSON object, `workflow_state.cjs:1523-1528`), so this is informational. | No action in this plan (Step 1 artifact is frozen; the refined plan is the plan of record). Orchestrator may rebuild `plan.index.json` from the refined plan if Step 3/4 needs a fresh snapshot; if a later gate pins the index sha (ledger `--plan-index`), link after any rebuild. | project (hash comparison + code read) | `.runtime/plan.index.json:4-8`; `workflow_state.cjs:1816,1523-1528` | — |

## Project-context sweep (Grilling Protocol step 1)

Consulted before applying any default:

| Source | Used for |
|--------|----------|
| `.agents/skills/ws-shared/runtime/scripts/workflow_state.cjs` (op allowlists 1441/2049, revision 1460, fingerprint 1454, dual-write 581/1808, validateSnapshot 1921, serializeFrontmatter 534) | Q9, G1, G4, G10, G11 |
| `.agents/skills/ws-monitor/scripts/monitor_snapshot.cjs` (limits 62-66, scan 1132-1230, resolve 1300-1326, roots 1031-1100, muse expand 97-122, stall loop 1495-1522, parseArgs 290, main 1660) | Q4, Q6, G5–G8, G13, G16 |
| `telemetry.schema.json` (enum line 9, `additionalProperties:false` line 73) / `workflow-state.schema.json` (`additionalProperties:true` root, line 173) | Q1/Q5, G10 |
| `test/test-ws-monitor-us356.js` (stall ~286, `scan-capped` ~401, capped block ~478-500) | Q6, G6, G12 |
| `test/test-suites.json` (`local` 50 / `remote` 34 / `harnessEfficiency` 76; monitor suites live in `harnessEfficiency`) | G18-style registration in refined §3.8 |
| `ws-spec-to-pr/SKILL.md` (Pause/Resume row :57, autoMode rows :29/:44) / `PROTOCOLS.md` (:31, :88, :227, :255, :281) / `gates.md` (:37, :52, :139) / `setup.md` § Resume (:171-211) | Q3, Q8, G14 |
| `ws-shared/runtime/observer-instructions.md` (transcript vocabulary, finding classification) / `ws-monitor/SKILL.md` (signal map :126, transcript source :113) | AC17, G8, G14 |
| `CROSS-PLATFORM.md` § PowerShell rules; `.ws/MEMORY.md` + `.ws/memory/*`; spec-memo vault bootstrap | G2, G12, G15 |
| `.agents/plans/us-412-413-liveness-checkpoints/observer/window-1-report.md` (F1–F5 dogfood evidence) | G6, G7, refined M-tests |
| Legacy states + `PROTOCOLS.md` state shape (`checkpoints[]`) | G1 |

## Memory consult

| Backend | Keywords | Hits applied |
|---------|----------|--------------|
| Local files (`.ws/MEMORY.md`, 198 entries; `.ws/memory/*`) | checkpoint, turn boundary, subagent turn continuation, monitor transcript, stall, integrity, version bump, PowerShell quoting, state writer | `2026-09-18-subagent-turn-continuation` (turn rule; step-output contract), `2026-09-21-monitor-subagent-recovery-correlation` (monitor fixture isolation), `2026-09-09-integrity-after-final-skill-edit`, `2026-09-22-us-395-terminal-close-and-integrity`, `2026-09-19-ship-version-bump-when-equal-base`, `2026-09-22-us-402-ship-verify-order`, `2026-09-21-g2-completeness-ledger-boundary`, `2026-09-22-us-386-powershell-cli-vocabulary`, `2026-09-21-powershell-quoting-one-liners`, `2026-09-21-skills-root-package-membership`, `2026-09-03-plans-index-per-workflow-updatedat` (state-writer must refresh the plans index row) |
| spec-memo vault (`bootstrap`, hybrid) | us-412-413 liveness checkpoints plan interview | 3 active traps returned (`g2-code-slug-files-touched-only`, `spec-to-pr-no-harness-benchmark`, `ws-spec-memo-bridge-not-vault-catalog`) — none contradict this plan; FTS search for liveness/checkpoint/stall terms returned no additional records |

## DoR / Section 6 audit

| Check | Result |
|-------|--------|
| Every section 0–8 scanned | Yes; findings registered (Q1–Q9 + G1–G16) |
| Failing-test baseline per regression AC | AC10–AC16 have NS1–NS4 with named red-before assertions (M1–M7); AC1–AC7/AC9 have T1–T7 including NS5/NS6; AC18 binds them to suites + registration |
| Section 6 (Stack & Security Invariants) present and mandated | Yes — refined plan §6 keeps the boundary table and adds the G8 privacy clause and G10 validator-limitation note |
| Touched framework boundaries have explicit checks | State-writer integrity (revision/dual-write/index/validate), schema/input validation, observer read-only, portability, consumer-data isolation — all named with verifying tests |
| AC mapping preserved | AC1–AC19 mapped in refined §1 and §5; no AC dropped or weakened by the resolutions (G1 rename is AC-compatible) |
| Visual references | Spec has none; N/A |

## Refined-plan deltas vs step-01 (authoritative in the refined artifact)

1. `state.checkpoints` → **`state.stepCheckpoints`** (G1), with the legacy `checkpoints[]` git-tag mirror explicitly left alone.
2. `--progress-file <path>` added beside `--progress` (G2); docs use the file form, tests use argv arrays.
3. `capped` includes per-root slice truncation (G6) — protects `test-ws-monitor-us356.js` and keeps `scan-capped` honest.
4. Root ordering: correlated-path → explicit → config → workspace → host (G7).
5. One sanitized shared window for filter, store, and resolve (G8); `delete scanned.tail` retained.
6. Pause-write discipline stated as a contract rule; `dispatch` never touches markers (G9); `finish` idempotency regression (G4).
7. Schema declarations shallow + code-level record validation (G10); md round-trip assertion (G11).
8. New tests T8 (`--progress-file` parity/fail-closed) and T9 (dispatch-no-marker + idempotent finish), M9 (per-root truncation `capped`); registration kept in `local`+`remote` (state) and `harnessEfficiency` (monitor).
9. Upstream obligation ordering (G15) and sibling-sweep extension (G16) spelled out in §3/§7.
