# Implementation plan — fix-pr-batch-plan-exec

**Spec:** `.agents/specs/fix-pr-batch-plan-exec.spec.md`  
**Context:** `.agents/specs/fix-pr-batch-plan-exec.context.md`  
**Registered:** `.agents/plans/fix-pr-batch-plan-exec/step-00-fix-pr-batch-plan-exec.spec.md`

## Approach

Mirror Step 6 review → `reviewFix` inside each fix-pr **batch** (Act round / standalone `/fix-pr`): plan under `fixPrPlan` → `reviewerModel`, then execute under `fixPrExec` → `executionModel`. No new FSM step.

## Tasks

1. **Config / resolve docs** — Add `fixPrPlan` / `fixPrExec` to `config.schema.json`, example presets, `tools.md`, `STEP-DISPATCH.md` / `PROTOCOLS.md`, `ws-configure-project` interview; wire resolve fallbacks.
2. **`ws-fix-pr`** — Split score/plan vs surgical/exec; plan forbids product edits; exec follows plan-gate + existing `COOPERATIVE_FIX` / learning.
3. **`ws-goal-fix-pr`** — Act round Done-when requires both substeps before push/resolve.
4. **Lite** — Plan-before-edit order only; session model (no dual `dispatch-agent`).
5. **Tests / evals** — Assert role names, fallbacks, plan-before-edit; leave `AUTO_FIX.md` alone.
6. **Ship hygiene** — Integrity regenerate, `ws-check-harness`, version/docs when implementing.

## Verify

- Authoring validate on the registered step-00 copy
- Focused test(s) for role strings + skill prose
- Manual dry-run of one Act round: plan gate written with no product diff, then exec
