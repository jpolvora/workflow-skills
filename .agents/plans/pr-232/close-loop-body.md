Merged https://github.com/jpolvora/workflow-skills/pull/232 via ws-spec-to-pr Step 9.

PR 232 (`fix: hybrid measure_harness skill resolve`) had zero open review threads and green `review` + `test` checks on first read. No Act round. Merge used `--merge` without deleting `develop`.
