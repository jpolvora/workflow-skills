---
step: 6
slug: specialized-subagents-compiler
workflowId: specialized-subagents-compiler-20260909T120930Z
status: active
startedAt: "2026-09-09T12:09:30Z"
endedAt: "2026-09-09T12:53:26.579Z"
acRefs: []
---
# Code Review Report: Specialized Subagents Compiler

**Workflow ID:** `specialized-subagents-compiler-20260909T120930Z`
**Slug:** `specialized-subagents-compiler`
**Base Commit:** `14928a7018266ee857db6f5608b0538e92ada4a2`
**Head Commit:** `e44cc96e6d9a81a02e32b4733c534015c27d5f98`
**Review Round:** 1 (Approved)
**Verdict:** `APPROVED`

---

## 1. Summary of Changes

This pull request introduces the optional compiler and host projection mechanism for specialized workflow subagents (`compile_host_subagents.cjs`), allowing opt-in materialization of canonical skills into host-native specialized subagent definitions (`.cursor/agents/ws-step-*.md`).

Key components reviewed:
1. **Schema & Configuration (`config.schema.json`, `config.json.example`):**
   - Added `defaults.specializedSubagents` with properties `enabled` (boolean, default: `false`), `targetHost` (enum: `["auto", "cursor", "claude", "generic"]`, default: `"auto"`), and `agentPrefix` (string, default: `"ws"`).
2. **Hub Classification (`hub-layout.json`):**
   - Added `generatedHostProjections` classification for `.cursor/agents` with `sourceControl: "optional-track-or-ignore"`.
3. **Compiler Implementation (`compile_host_subagents.cjs`):**
   - Implemented standalone CLI compiler with complete argument support (`--repo-root`, `--host`, `--prefix`, `--clean`, `--check`, `--json`, `--force`).
   - Generates all 10 workflow step agents (Steps 00–09) with appropriate frontmatter (`name`, restricted `description`, `disable-model-invocation: true`, `readonly: true` for Step 05).
   - Generates and validates cryptographic signature header `<!-- @generated by workflow-skills compile_host_subagents.cjs. DO NOT EDIT DIRECTLY. sha256:... -->`.
   - Protects custom non-generated files from accidental overwrites without `--force`.
   - Implements drift detection in `--check` mode and clean uninstallation in `--clean` mode.
4. **Project Configuration Integration (`auto_configure.cjs`, `SKILL.md`, `INTERVIEW.md`):**
   - Added `--section specializedSubagents` and automatic host detection (`cursor` if `.cursor/` or `.cursorrules` exists, else `auto`).
   - Auto-triggers compiler immediately upon enabling specialized subagents.
5. **Runtime Dispatch Protocol (`host-dispatch.md`):**
   - Documented Tier 1 named specialized subagent dispatch, zero-turn bootstrap payload with discrete context pointers, and fail-safe fallback to generic or inline execution.
6. **Automated Unit Tests (`test/test-specialized-subagents-compiler.js`):**
   - 9 comprehensive automated tests verifying schema validation, unsupported host rejection, generation, frontmatter, drift detection, clean mode, collision protection, and auto-configure integration.

---

## 2. Adversarial Review Checklist

- [x] **Neutrality & Portability:** Core workflow skills (`ws-*`) do not contain any IDE-specific branding or required host layouts. Host projections live strictly in compiled projection directories (`.cursor/agents/`).
- [x] **Zero-Turn Bootstrap & Token Economy:** Subagent system prompts embed all instructions, invariants, and output formats, allowing orchestrators to transmit discrete context pointers only.
- [x] **Safety & Non-Destructive Operation:** Custom user files in `.cursor/agents/` are safely preserved during `--clean` and protected from overwrite during compile.
- [x] **Fail-Safe Fallback:** If specialized subagents are disabled, missing, or error during dispatch, execution falls back seamlessly without aborting workflow runs.
- [x] **Stack Invariants & Static Quality:** Static scan (`scan_stack_invariants.cjs`) reports 0 critical issues and 0 warnings.
- [x] **Package Integrity:** Manifest `bin/skill-integrity.json` matches all source digests.

---

## 3. Findings & Observations

- **Critical:** None.
- **Warnings:** None.
- **Suggestions:** None.

---

## 4. Final Verdict

**APPROVED** — The implementation is complete, clean, thoroughly tested, and ready to advance.
