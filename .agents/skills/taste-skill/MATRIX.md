# taste-skill × Matrix

Matrix register is **product** (operations console). taste-skill targets landing pages and portfolios — use it here for **anti-slop discipline**, not marketing layouts.

## When to load

| Surface | Skill |
|---------|--------|
| Admin / member / platform app shell (`web/src/components/layout`, shared UI) | [impeccable](.cursor/skills/impeccable/SKILL.md) + taste-skill **§0 Brief inference**, **§4.4–4.6** (materiality, states, forms) |
| Tenant public sites (`web/src/pages/public-sites`) | taste-skill full + [impeccable](.cursor/skills/impeccable/SKILL.md) brand register |
| Redesign / anti-slop pass on existing UI | taste-skill audit-first + impeccable `polish` / `quieter` |

## Matrix design read (default)

*Reading this as: multi-tenant B2B2C operations console for admins and members, with Linear-style restrained product language, leaning toward token-driven shell + theme parity, anti-slop by keeping decorative flair out of `data-app-shell` chrome.*

**Dials for Matrix app UI:** VARIANCE 4 · MOTION 3 · DENSITY 7

Skip: centered heroes, bento grids, eyebrow sections, gradient metric text on wallet/ledger.
