{
  "skill_name": "ws-monitor",
  "evals": [
    {
      "id": 1,
      "prompt": "Use @ws-monitor for a typical monitor task in this project.",
      "expected_output": "Agent loads ws-monitor and follows its skill contract.",
      "assertions": [
        "Agent loads ws-monitor before acting",
        "Follows skill steps and Done when criteria",
        "Uses path tokens from tools.md instead of hardcoded consumer paths",
        "Output is en-us and harness-neutral"
      ]
    },
    {
      "id": 2,
      "prompt": "/monitor - edge case: missing or incomplete config.json",
      "expected_output": "Agent stops or bootstraps via configure-project/setup, not silent guessing.",
      "assertions": [
        "Recognizes ws-monitor trigger from slash or @ invocation",
        "References ws-shared/config.json or configure-project when config missing",
        "Does not invent project-specific metadata"
      ]
    },
    {
      "id": 3,
      "prompt": "/monitor - a standard run with every step terminal (Step 3 skipped dag-disabled) still reports status: active with endedAt: null",
      "expected_output": "Agent reports terminal-run-active, derives a terminal reported status so the run is not counted active, and points at the stale state file.",
      "assertions": [
        "Emits the terminal-run-active finding for a terminal-shaped run still reporting active with endedAt null",
        "Does not count a terminal-shaped run in activeCount or report status == active for it",
        "Explains the terminal shape (all steps through the close step terminal) and cites the state file as evidence",
        "Does not modify the state file (read-only observation)"
      ]
    },
    {
      "id": 4,
      "prompt": "/monitor - a ws-spec-multi run whose in_progress row's child worker already closed, or a completed run that still lists pending rows",
      "expected_output": "Agent reports stale-parent-row for the completed-child / non-terminal parent-row shape.",
      "assertions": [
        "Emits stale-parent-row when a queue row is non-terminal while its child workflow is terminal",
        "Emits stale-parent-row when a terminal run still lists pending/in_progress rows (phantom rows)",
        "Emits stale-parent-row when a newer active run claims the same in_progress slug (supersede never retired)",
        "Names the affected slug and cites the queue state file as evidence"
      ]
    },
    {
      "id": 5,
      "prompt": "/monitor - a ws-spec-multi run whose shipped/in_progress item left no child state under {plansDir}/{slug}/",
      "expected_output": "Agent reports missing-child-state, and the multi-spec expectedArtifacts list is populated from the queue rows instead of empty.",
      "assertions": [
        "Emits missing-child-state for an in_progress/shipped/failed item with no child workflow state under {plansDir}/{slug}/",
        "Requires a parseable machine SoT *.state.json with the workflow-identity fields (a .state.md render or invalid JSON does not count) and ignores the reserved ws-spec-multi batch directory alias",
        "Populates the ws-spec-multi expectedArtifacts list from the batch queue rows (never an empty list)",
        "Stays silent for a pending/skipped item and for an advanced item whose child state exists",
        "Does not duplicate stale-parent-row, which requires a child that already closed"
      ]
    }
  ]
}
