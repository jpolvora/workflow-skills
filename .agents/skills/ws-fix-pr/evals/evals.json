{
  "skill_name": "ws-fix-pr",
  "evals": [
    {
      "id": 1,
      "prompt": "Fix open review threads on PR #42 for oauth-login.",
      "expected_output": "Threads addressed with evidence; uses provider scripts via shims.",
      "assertions": [
        "Agent loads ws-fix-pr",
        "Uses fetch_threads/resolve_thread via documented script paths",
        "Does not claim threads resolved without checking remote state"
      ]
    },
    {
      "id": 2,
      "prompt": "resolve all PR comments automatically",
      "expected_output": "Cooperative fix flow per skill; no blind auto-resolve without reading threads.",
      "assertions": [
        "Agent reads thread content before resolving",
        "Uses provider-appropriate SCM from config",
        "Uses path tokens ({skillsRoot}, {sharedDir}, {plansDir}) instead of hardcoded consumer paths"
      ]
    },
    {
      "id": 3,
      "prompt": "A review thread on validate-evals.cjs says the gate claims schema validation but never reads the schema, and notes the same pattern in a copied helper.",
      "expected_output": "Defect class named; proactive discovery; both occurrences fixed or skipped with path+reason before resolve.",
      "assertions": [
        "Agent names a defect class, not only the anchored file:line",
        "Agent runs proactive discovery (code grep and context) before resolving the thread",
        "Agent does not close the thread while named sibling paths remain unfixed without an exemption"
      ]
    },
    {
      "id": 4,
      "prompt": "A review thread on src/auth/token.cjs flags missing null checks; grep finds the same unchecked access in src/auth/refresh.cjs (not named in the thread).",
      "expected_output": "Second same-class path fixed or explicitly skipped with path+reason; proactive report fields recorded before resolve.",
      "assertions": [
        "Agent fixes refresh.cjs or records proactiveSkipped with path and reason",
        "Agent records defectClass and proactiveFixed or proactiveSkipped on plan-gate or resolution",
        "Agent does not resolve after fixing only token.cjs when refresh.cjs remains unfixed without a skip"
      ]
    },
    {
      "id": 5,
      "prompt": "A review thread flags a small typo in one helper; proactive grep finds the same typo class in a large legacy module that would require a cross-package refactor.",
      "expected_output": "Small hit fixed; large hit recorded under proactiveSkipped with path+reason; class not marked fully cleared.",
      "assertions": [
        "Agent applies the size gate: fixes the local hit and skips the large refactor with an explicit reason",
        "Agent lists the skipped path under proactiveSkipped, not silent omission",
        "Agent does not claim the defect class is fully cleared when a same-class hit was skipped"
      ]
    },
    {
      "id": 6,
      "prompt": "Fix PR threads in a consumer repo with no MEMORY.md under ws-shared.",
      "expected_output": "Fix-pr proceeds; MEMORY consult-skipped recorded in sourcesConsulted; not a fatal error.",
      "assertions": [
        "Agent does not abort fix-pr because MEMORY.md is missing",
        "Agent records memory consult-skipped in sourcesConsulted",
        "Agent still runs code and context proactive discovery before resolve"
      ]
    },
    {
      "id": 7,
      "prompt": "A code-reviewer CI thread scored 8 flags a missing null check that we just shipped; fix it and finish the pass.",
      "expected_output": "Surgical fix plus a MEMORY trap so the next round does not repeat the mistake; Learning: N/A is forbidden for this accepted defect.",
      "assertions": [
        "Agent writes a memory/YYYY-MM-DD-fix-pr-*.md trap (or cites an existing Medium+ hit) after fixing the accepted reviewer/CI defect",
        "Agent compiles MEMORY after the write",
        "Agent does not use Learning: N/A for an accepted score 6-10 reviewer/CI defect"
      ]
    }
  ]
}
