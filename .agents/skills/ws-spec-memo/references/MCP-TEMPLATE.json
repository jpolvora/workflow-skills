{
  "_comment": "Register in your agent host MCP settings. Replace command/args if specMemo.cli uses npx.",
  "mcpServers": {
    "spec-memo": {
      "command": "memo",
      "args": ["serve"]
    }
  }
}
