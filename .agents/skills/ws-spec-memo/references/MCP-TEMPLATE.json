{
  "_comment": "Pick the block matching specMemo.cli from config.json. Use spec-memo-npx when cli is npx -y spec-memo.",
  "mcpServers": {
    "spec-memo-global": {
      "command": "memo",
      "args": ["serve"]
    },
    "spec-memo-npx": {
      "command": "npx",
      "args": ["-y", "spec-memo", "serve"]
    }
  }
}
