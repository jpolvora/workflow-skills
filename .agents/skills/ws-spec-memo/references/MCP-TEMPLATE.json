{
  "_comment": "Pick the block matching specMemo.cli from config.json. Use spec-memo-npx when cli is npx -y spec-memo. For SSE (port 3000), status monitor (3001), and canvas (4100), see spec-memo ws-memo/references/MCP-TEMPLATE.json — keep stdio memo serve as the default here.",
  "mcpServers": {
    "spec-memo-global": {
      "command": "memo",
      "args": ["serve"]
    },
    "spec-memo-npx": {
      "command": "npx",
      "args": ["-y", "spec-memo", "serve"]
    }
  }
}
