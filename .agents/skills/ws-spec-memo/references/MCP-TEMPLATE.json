{
  "_comment": "Canonical keys match specMemo.mcpServerName default (spec-memo) and spec-memo ws-memo/references/MCP-TEMPLATE.json. Use spec-memo-npx when cli is npx. Prefer npx -y github:jpolvora/spec-memo; npm package `npx -y spec-memo serve` is an alternate. SSE/status/canvas ports and serve flags live in the spec-memo ws-memo template — do not duplicate them here.",
  "mcpServers": {
    "spec-memo": {
      "command": "memo",
      "args": ["serve"]
    },
    "spec-memo-npx": {
      "command": "npx",
      "args": ["-y", "github:jpolvora/spec-memo", "serve"]
    }
  }
}
