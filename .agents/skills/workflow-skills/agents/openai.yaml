interface:
  display_name: "Workflow Skills"
  short_description: "Repo-specific patterns and workflows for workflow-skills"
  default_prompt: "Use the workflow-skills repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true